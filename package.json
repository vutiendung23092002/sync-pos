{
  "name": "pos-lark-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "sync": "node src/index.js",
    "schema:check": "node scripts/sync-lark-schema.js",
    "schema:apply": "node scripts/sync-lark-schema.js --apply",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "pg": "^8.16.0",
    "pino": "^9.7.0",
    "pino-pretty": "^13.1.1"
  }
}
